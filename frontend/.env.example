# Optional build-time API origin for direct browser calls.
# Leave this blank/unset when using the recommended Cloudflare Pages Function
# proxy, then configure BACKEND_URL in Cloudflare Pages instead.
REACT_APP_BACKEND_URL=
